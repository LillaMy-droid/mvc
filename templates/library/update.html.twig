{% extends 'base.html.twig' %}

{% block title %}Delete book{% endblock %}

{% block main %}

<h1>Choose book to update</h1>
    <form action="{{ path('update_book_form') }}" method="GET">
        <label for="Isbn">Välj bok:</label>
        <select name="Isbn" id="Isbn">
            {% for book in library %}
                <option value="{{ book.Isbn }}">{{ book.titel }} By {{ book.author }}</option>
            {% endfor %}
        </select>
        <button type="submit">Update book</button>
    </form>
{% endblock %}